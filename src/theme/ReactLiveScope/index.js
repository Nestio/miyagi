import React from 'react';

// DEPRECATED: This was for Dyte UI Kit live code examples
// Funnel Leasing docs don't use Dyte UI Kit components
// Keeping minimal scope for react-live if needed for other examples

const ReactLiveScope = {
  React,
  ...React,
  // Add any Funnel Leasing-specific components here if needed for live code examples
};

export default ReactLiveScope;
