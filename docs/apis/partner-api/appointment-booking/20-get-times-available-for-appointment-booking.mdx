---
title: Get times available for appointment booking
sidebar_label: Get times available for appointment booking
sidebar_position: 21
hide_table_of_contents: true
---

# Get times available for appointment booking

{/* Define attributes and code as variables */}
{(() => {
  const requestAttributes = [];

  const responseAttributes = [
  {
    "name": "available_times",
    "type": "array<string>",
    "description": "",
    "required": false,
    "nullable": false
  }
];

  const requestExampleCode = "{}";

  const responseExampleCode = "{\n  \"available_times\": [\n    \"2020-01-07T09:00:00\"\n  ]\n}";

  return (
    <ApiPage
      title="Get times available for appointment booking"
      description="Returns a list of times that are avaialble for a booking on the given date. Any of these times should be a valid value for start field when booking an appointment using the booking end-point. "
      method="GET"
      path="/api/partners/v1/community/:community_id/appointments/available-times/"
      requestAttributes={requestAttributes.length > 0 ? requestAttributes : undefined}
      requestExampleCode={requestAttributes.length > 0 ? requestExampleCode : undefined}
      requestExampleTitle="Example request"
      requestMethod="GET"
      requestPath="/api/partners/v1/community/:community_id/appointments/available-times/"
      attributes={responseAttributes}
      exampleCode={responseExampleCode}
      exampleTitle="Example response"
      exampleLang="json"
    />
  );
})()}
