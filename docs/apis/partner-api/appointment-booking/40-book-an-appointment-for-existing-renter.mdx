---
title: Book an appointment (for existing renter)
sidebar_label: Book an appointment (for existing renter)
sidebar_position: 23
hide_table_of_contents: true
---

# Book an appointment (for existing renter)

{/* Define attributes and code as variables */}
{(() => {
  const requestAttributes = [
  {
    "name": "start",
    "type": "string",
    "description": "",
    "required": false,
    "nullable": false
  },
  {
    "name": "message",
    "type": "string",
    "description": "",
    "required": false,
    "nullable": false
  },
  {
    "name": "tour_type",
    "type": "string",
    "description": "",
    "required": false,
    "nullable": false
  },
  {
    "name": "pms_unit_ids",
    "type": "array<string>",
    "description": "",
    "required": false,
    "nullable": false,
    "example": "123,5678"
  }
];

  const responseAttributes = [];

  const requestExampleCode = "{\n  \"start\": \"string\",\n  \"message\": \"string\",\n  \"tour_type\": \"string\",\n  \"pms_unit_ids\": [\n    \"123\",\n    \"5678\"\n  ]\n}";

  const responseExampleCode = "{}";

  return (
    <ApiPage
      title="Book an appointment (for existing renter)"
      description="Create an appointment(tour) in Funnel for the specified renter for the specified community"
      method="POST"
      path="/api/partners/v1/community/:community_id/renters/:funnel_renter_id/appointment/"
      requestAttributes={requestAttributes.length > 0 ? requestAttributes : undefined}
      requestExampleCode={requestAttributes.length > 0 ? requestExampleCode : undefined}
      requestExampleTitle="Example request"
      requestMethod="POST"
      requestPath="/api/partners/v1/community/:community_id/renters/:funnel_renter_id/appointment/"
      attributes={responseAttributes}
      exampleCode={responseExampleCode}
      exampleTitle="Example response"
      exampleLang="json"
    />
  );
})()}
