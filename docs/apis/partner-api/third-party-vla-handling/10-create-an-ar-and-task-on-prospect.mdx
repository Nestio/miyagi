---
title: Create an AR and Task on Prospect
sidebar_label: Create an AR and Task on Prospect
sidebar_position: 50
hide_table_of_contents: true
---

# Create an AR and Task on Prospect

{/* Define attributes and code as variables */}
{(() => {
  const requestAttributes = [
  {
    "name": "funnel_last_communication_id",
    "type": "string",
    "description": "",
    "required": false,
    "nullable": false
  },
  {
    "name": "disable",
    "type": "boolean",
    "description": "disables third party vla handling",
    "required": false,
    "nullable": false,
    "example": "false"
  },
  {
    "name": "due_date",
    "type": "string",
    "description": "must follow ISO 8601 and include offset",
    "required": false,
    "nullable": false,
    "example": "2023-05-16T14:30:00-04:00"
  },
  {
    "name": "task_description",
    "type": "string",
    "description": "Description to be added to the reminder text for the action_required task",
    "required": false,
    "nullable": false
  }
];

  const responseAttributes = [
  {
    "name": "message",
    "type": "string",
    "description": "",
    "required": false,
    "nullable": false,
    "example": "Successfully added task"
  }
];

  const requestExampleCode = "{\n  \"funnel_last_communication_id\": \"string\",\n  \"disable\": false,\n  \"due_date\": \"2023-05-16T14:30:00-04:00\",\n  \"task_description\": \"string\"\n}";

  const responseExampleCode = "{\n  \"message\": \"Successfully added task\"\n}";

  return (
    <ApiPage
      title="Create an AR and Task on Prospect"
      description="Create an action_required and task in funnel and disable third party vla, if specified in request"
      method="POST"
      path="/api/partners/v1/community/:community_id/renters/:funnel_renter_id/action-required/"
      requestAttributes={requestAttributes.length > 0 ? requestAttributes : undefined}
      requestExampleCode={requestAttributes.length > 0 ? requestExampleCode : undefined}
      requestExampleTitle="Example request"
      requestMethod="POST"
      requestPath="/api/partners/v1/community/:community_id/renters/:funnel_renter_id/action-required/"
      attributes={responseAttributes}
      exampleCode={responseExampleCode}
      exampleTitle="Example response"
      exampleLang="json"
    />
  );
})()}
